<!--miniprogram/pages/course/course.wxml-->
<view>
    <view class="allcourse">
        <view bindtap="toVideoTrain" data-course_type="{{item.course_type}}" wx:for="{{allcourse}}">
            <view class="dd_mask"></view>
            <image src="{{item.imgSrc}}"></image>
            <view class="allcourse-name ">{{item.course_type}}</view>
        </view>
    </view>
</view>

